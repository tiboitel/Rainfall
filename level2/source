#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void	p(void)
{
	char	*buffer;
	char	*ptr;

	fflush(stdout);
	gets(buffer);
	if ((unsigned int)ptr & 0xb0000000) == 0xb0000000) 
	{
		printf("(%p)\n", addr);
		exit(1);
	}
	puts(buffer);
	strdup(buffer);
}

int		main(int argc, char **argv)
{
	p();
	return (0);
}
