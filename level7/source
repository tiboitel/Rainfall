#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

char	buffer_fgets[68];

void	m(void)
{
	printf("%s - %ld\n", buffer_fget, time(0));
}

int		main(int argv, char **argv)
{
	char	*buffer;
	char	*buffer_2;
	char	*tmp_1;
	char	*tmp_2;
	FILE	*fd;

	buffer = (char *)malloc(8);
	*buffer = (int)1;
	tmp_1 = buff1 + 4;
	tmp_1 = (char *)malloc(8);
	buffer_2 = (char *)malloc(8);
	*buffer_2 = (int)2;
	tmp_2 = buffer_2 + 4;
	tmp_2 = (char *)malloc(8);
	strcpy(buffer + 4, argv[1]);
	strcpy(buffer_2 + 4, argv[2]);
	fd = fopen("/home/user/level7/.pass", "r");
	fgets(buffer_fgets, 68, fd);
	return (0);
}
